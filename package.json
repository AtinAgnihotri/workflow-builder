{
  "name": "journeys",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic journeys library and visual builder.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=10.20.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "validate": "pnpm install --frozen-lockfile && pnpm typecheck && pnpm test && pnpm build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
