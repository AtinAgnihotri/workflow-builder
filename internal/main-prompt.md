You are a retired Hashimoto type PE level techie and you have been asked to implement the following.

> **Note:** This project shipped as **Journeys** (`@journeys/core` on npm). The original prompt below uses "workflow builder" as the working name.

We are going to create something called Journeys, the idea being that people can create workflows, whether those workflows be journeys through a UI, a rules engine, or some other kind, with the flow being represented as a graph Adjaceny list, with each node of the graph being a point in the workflow, and the edges being weighted edges where weights on these edges are a group of operands, like "name" == "John Doe", "age" >= 29, so forth, and "address" CONTAINS "London". The name of the key can be any string that can be a key in JSON, the value of operand be any operation that make sense in JS, and the type of value being aligned to base types in JS where these operations can be applied. 

That's one of the main callouts, to represent this we are not going to invent a DSL, we are going to implement this in such a way that the Adjacency list comes out as a JSON.

This is going to be a TS library that one can take and publish to NPM, with the idea being it's something that is platform or framework agnostic and can be run anywhere base JS is run (browser, Node, cloudflare workers)

We are also going to build a UI where a non-technical person can create a this workflow in a UI and it spit out a JSON that a developer can wire-up to their use-case, manually or through a pipeline

Once the base TS package is implemented, we are going to think and if necessary build adapters, for things like React, Solid, Swelte, Vue, etc, their famous implementation like CSR routers like React Router or Tanstack Router, or even metaframeworks like Next and Tanstack start, the idea being that bring whatever framework, metaframework, Reactivity model, state management (zustand, jotai, etc), one dev should be able to wire it up and give it off to product to switch around the flow of a user journey (of course adding new inputs, mapping state items to values for workflow builder operands to work on, eg, useState.loanApplications.[0].lvr -> loanValueRatio in Journeys is something dev does, putting rules like first loan LVR being less then 70 takes user to page 1, or shows card 1, and second page LVR takes user to page2 / shows card 2 is something product does, and the product is able to change the threshold from 70 to 80 without going back to the developer). If you think as a Principal Engineer level techie that it doesn't make sense to write adapters, but just show some examples of how it can be implemented we go with that.

Finally you also document a strategy of how to surface this as a open source tool to float and test this tool (say Discord of PrimeAgen, Theo, etc, or forums that OSS TS devs frequent), a GH pages in docs/ for, well, Docs, which license make sense so that Enterprise Devs are not afraid of adopting this model, while the idea of credit is preserved, and finally if there's a way to ask for donations if people bite while not being obnoxious about it.

I want you to implement think about all these aspects and write docs and PRPs first and foremost, and do that in a very detailed manner (like spelling out stuff in psuedocode if you have to) since the implementation will be carried out by Cursor's Composer 2.5 Fast, which is a less capable model, specially as far as reasoning effort is concerned